class Environment {
    public:
        Environment() = default;

        void update() {
            // update the positions of all objects
            // gravity, acceleration, position, etc
        }
}